help: Show log for a specified firewall (IPv6)
allowed: /bin/cli-shell-api listActiveNodes firewall ipv6-name | sed -e "s/'//g" && echo
run: cat $(printf "%s\n" /var/log/messages* | sort -nr ) | egrep "\[$5-([0-9]+|default)-[ADR]\]"
