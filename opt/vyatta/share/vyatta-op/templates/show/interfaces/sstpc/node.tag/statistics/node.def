help: Show specified SSTP client interface statistics
allowed: /bin/cli-shell-api listActiveNodes interfaces sstpc | sed -e "s/'//g" && echo
run: if [ -d "/sys/class/net/$4" ]; then /usr/sbin/pppstats "$4"; fi
