type: txt
help: Bonding transmit hash policy (default: layer2)
val_help: layer2; use MAC addresses to generate the hash (default)
val_help: layer2+3; combine MAC address and IP address to make hash
val_help: layer3+4; combine IP address and port to make hash
val_help: encap2+3; combine encapsulated MAC address and IP address to make hash
val_help: encap3+4; combine encapsulated IP address and port to make hash
allowed: echo "layer2 layer2+3 layer3+4 encap2+3 encap3+4"
syntax:expression: exec "${vyos_libexec_dir}/validate-value --regex \'(layer2\\+3|layer3\\+4|layer2|encap2\\+3|encap3\\+4)\'   --value \'$VAR(@)\'"; "hash-policy must be layer2 layer2+3 layer3+4 encap2+3 or encap3+4"
