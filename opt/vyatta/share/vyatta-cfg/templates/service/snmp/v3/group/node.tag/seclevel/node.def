type: txt
help: Security levels (default: auth)
val_help: noauth; Messages not authenticated and not encrypted (noAuthNoPriv)
val_help: auth; Messages are authenticated but not encrypted (authNoPriv) (default)
val_help: priv; Messages are authenticated and encrypted (authPriv)
allowed: echo "noauth auth priv"
syntax:expression: exec "${vyos_libexec_dir}/validate-value --regex \'(noauth|auth|priv)\'   --value \'$VAR(@)\'"; "Invalid value"
