type: txt
help: Set period to rescan BGP table to check if condition is met
			(default: 60)
val_help: u32:5-240; Period to rerun the conditional advertisement scanner process
syntax:expression: exec "${vyos_libexec_dir}/validate-value  --exec \"${vyos_validators_dir}/numeric --range 5-240\"  --value \'$VAR(@)\'"; "Invalid value"
